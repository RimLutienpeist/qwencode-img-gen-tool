# 智能尺寸处理示例

本文档展示不同目标尺寸下，工具的智能处理策略。

## 📏 尺寸处理规则

- **512x512 - 2048x2048**：直接使用目标尺寸调用 API（最佳质量）
- **< 512x512**：放大到范围内生成，然后缩小
- **> 2048x2048**：缩小到范围内生成，然后放大

## 🎯 具体示例

### 1. 小尺寸图标（< 512）

#### 示例 1：128x128 小图标
```
目标尺寸: 128x128
API 尺寸: 512x512
缩放因子: 4x
策略: 生成 512x512 → 缩小为 128x128
质量: ⭐⭐⭐⭐ (高质量缩小)
```

#### 示例 2：256x256 UI 图标
```
目标尺寸: 256x256
API 尺寸: 512x512
缩放因子: 2x
策略: 生成 512x512 → 缩小为 256x256
质量: ⭐⭐⭐⭐⭐ (完美)
```

#### 示例 3：64x64 超小图标
```
目标尺寸: 64x64
API 尺寸: 512x512
缩放因子: 8x
策略: 生成 512x512 → 缩小为 64x64
质量: ⭐⭐⭐ (可接受，建议使用矢量图)
```

---

### 2. 标准范围（512-2048）✅ 推荐

#### 示例 1：512x512 标准图标
```
目标尺寸: 512x512
API 尺寸: 512x512
缩放因子: 1x
策略: 直接使用，无需缩放
质量: ⭐⭐⭐⭐⭐ (原生质量)
```

#### 示例 2：1024x1024 标准素材
```
目标尺寸: 1024x1024
API 尺寸: 1024x1024
缩放因子: 1x
策略: 直接使用，无需缩放
质量: ⭐⭐⭐⭐⭐ (原生质量)
```

#### 示例 3：1920x1080 横向背景
```
目标尺寸: 1920x1080
API 尺寸: 1920x1080
缩放因子: 1x
策略: 直接使用，无需缩放
质量: ⭐⭐⭐⭐⭐ (原生质量)
```

#### 示例 4：2048x2048 高清素材
```
目标尺寸: 2048x2048
API 尺寸: 2048x2048
缩放因子: 1x
策略: 直接使用，无需缩放
质量: ⭐⭐⭐⭐⭐ (原生质量)
```

---

### 3. 大尺寸素材（> 2048）

#### 示例 1：4096x4096 4K 素材
```
目标尺寸: 4096x4096
API 尺寸: 2048x2048
缩放因子: 0.5x
策略: 生成 2048x2048 → 放大为 4096x4096
质量: ⭐⭐⭐⭐ (高质量放大)
```

#### 示例 2：3840x2160 4K 横向背景
```
目标尺寸: 3840x2160
API 尺寸: 2048x1152 (自动计算)
缩放因子: ~0.53x
策略: 生成 2048x1152 → 放大为 3840x2160
质量: ⭐⭐⭐⭐ (高质量放大)
```

#### 示例 3：8192x8192 超大素材
```
目标尺寸: 8192x8192
API 尺寸: 2048x2048
缩放因子: 0.25x
策略: 生成 2048x2048 → 放大为 8192x8192
质量: ⭐⭐⭐ (放大较多，建议分块生成)
```

---

## 📊 性能对比表

| 目标尺寸 | API 尺寸 | 后处理 | 生成时间 | 质量 | 推荐 |
|---------|---------|--------|---------|------|------|
| 128x128 | 512x512 | 缩小 4x | 标准 + 0.1s | ⭐⭐⭐⭐ | ⚠️ |
| 256x256 | 512x512 | 缩小 2x | 标准 + 0.1s | ⭐⭐⭐⭐⭐ | ✅ |
| 512x512 | 512x512 | 无 | 标准 | ⭐⭐⭐⭐⭐ | ✅✅ |
| 1024x1024 | 1024x1024 | 无 | 标准 | ⭐⭐⭐⭐⭐ | ✅✅ |
| 1920x1080 | 1920x1080 | 无 | 标准 | ⭐⭐⭐⭐⭐ | ✅✅ |
| 2048x2048 | 2048x2048 | 无 | 标准 | ⭐⭐⭐⭐⭐ | ✅✅ |
| 4096x4096 | 2048x2048 | 放大 2x | 标准 + 0.2s | ⭐⭐⭐⭐ | ⚠️ |
| 8192x8192 | 2048x2048 | 放大 4x | 标准 + 0.3s | ⭐⭐⭐ | ❌ |

---

## 🎨 使用建议

### 最佳实践

1. **优先使用 512-2048 范围**
   - 原生质量，无需后处理
   - 速度最快
   - 最推荐

2. **小图标（256-512）**
   - 适合 UI 元素、道具图标
   - 缩小质量很好

3. **大素材（2048-4096）**
   - 适合高清背景、CG 插画
   - 放大质量可接受

4. **避免极端尺寸**
   - 避免 < 128（太小，细节丢失）
   - 避免 > 4096（放大过多，建议分块或提高 API 尺寸）

### 特殊场景

#### 场景 1：需要超小图标（64x64）
```json
{
  "description": "金币图标",
  "category": "ui_asset",
  "size": "64x64"
}
```
**建议**：改为矢量图或使用 128x128

#### 场景 2：需要超大背景（8192x8192）
```json
{
  "description": "史诗级战场背景",
  "category": "background",
  "size": "8192x8192"
}
```
**建议**：
- 分为 4 块 2048x2048 生成
- 或使用 AI 放大工具（如 Real-ESRGAN）

#### 场景 3：非标准尺寸（1600x900）
```json
{
  "description": "游戏界面背景",
  "category": "background",
  "size": "1600x900"
}
```
**处理**：直接使用 1600x900 生成（在范围内）

---

## 🔬 技术细节

### 缩放算法

工具使用 **LANCZOS** 重采样算法：
- 高质量插值
- 保留细节
- 抗锯齿效果好

### 缩放因子计算

```python
# 示例：256x256 → 512x512
scale_factor = 2  # (512 / 256)

# 示例：4096x4096 → 2048x2048
scale_factor = 0.5  # (2048 / 4096)
```

### 2 的幂次优化

工具自动向上取整到 2 的幂次：
- 确保缩放质量
- 避免奇数缩放比例
- 例如：300x300 → 512x512 (1.7x → 2x)

---

**最后更新时间：** 2025-12-08
